# README: Модельный оркестратор и изолированные модели

## Структура

### `core/models/`
- `data_orchestrator.py` — централизованный обработчик данных, запускает модели в нужном порядке.
- `health_predictor.py` — модель физиологического состояния (на основе твоего кода), **изолирована** от других моделей, работает только с кэшем.
- `meteo_model.py` — заглушка для модели метео-влияний.
- `agro_model.py` — заглушка для агропрогноза.

## Что готово

- ✅ `data_orchestrator.py` — собирает данные, кэширует, запускает модели в нужном порядке.
- ✅ `health_predictor.py` — содержит **всю логику** из `Pasted_Text_1766259770304.txt`, но:
  - ✅ **НЕ вызывает** `meteo_model` или `agro_model`
  - ✅ **Получает** метео-данные **из кэша** (`local_db_weather`)
  - ✅ **Сохраняет** результаты в файлы (графики, CSV) в `data/`
  - ✅ **Использует** профиль пользователя из `central_db`
- ✅ `meteo_model.py` и `agro_model.py` — заглушки с интерфейсом для будущей реализации.

## Что нужно дореализовать

- 🔄 В `health_predictor.py`:
  - 🔧 **Конвертация кэшированных данных** из `local_db_weather` в `pandas.DataFrame` для модели.
  - 🔧 **Конвертация кэшированных данных** из `local_db_meteo` (турбулентность) в `pandas.DataFrame`.
- 🔄 В `meteo_model.py` и `agro_model.py`:
  - 🔧 Реализовать логику расчётов.
- 🔄 В `data_orchestrator.py`:
  - 🔧 Реализовать `fetch_gfs_turbulence_data` через `api_client`.
- 🔄 В `core/utils/api_client.py`:
  - 🔧 Добавить методы `get_gfs_data`, `get_hourly_forecast` (если ещё не реализованы).
- 🔄 В `core/db/local_db_weather.py` и `local_db_meteo.py`:
  - 🔧 Убедиться, что формат данных совместим с `health_predictor`.

## Архитектура

- Модели **не вызывают друг друга**.
- Все данные — через **кэш** (`local_db_*`).
- Оркестратор — **единая точка запуска**.
- Результаты — через **события** (`event_bus`).